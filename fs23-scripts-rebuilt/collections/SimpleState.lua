-- Reconstructed Luau source (luauc64 0.1.0).
-- This is a best-effort lift from bytecode; review before running.

SimpleState = {}
local v0 = Class(SimpleState)
function SimpleState.new(v0, v1, v2, v3)
  if not v3 then
  end
  v5(v6, v7)
  v4.id = v0
  v4.owner = v1
  v4.stateMachine = v2
  return v4
end
function SimpleState.delete(v0)
end
function SimpleState.activate(v0, v1)
end
function SimpleState.deactivate(v0)
end
function SimpleState.update(v0, v1)
end
