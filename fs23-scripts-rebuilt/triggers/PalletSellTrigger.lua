-- Reconstructed Luau source (luauc64 0.1.0).
-- This is a best-effort lift from bytecode; review before running.

PalletSellTrigger = {}
function PalletSellTrigger.onCreate(v0)
  Logging.error("PalletSellTrigger is not supported anymore. Use UnloadTrigger instead!")
end
